version: '3'
services:
  app:
    volumes:
      - ./app:/app
    build: ./app
    ports:
      - "8081:8081"
  sql:
    image: mysql:5.7
    volumes:
      - ./db:/docker-entrypoint-initdb.d
      - mysql-data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=1234

volumes:
  mysql-data: